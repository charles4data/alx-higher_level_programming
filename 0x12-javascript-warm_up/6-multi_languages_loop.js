#!/usr/bin/node

const languages = ['C', 'Python', 'JavaScript'];
const descriptions = ['fun', 'cool', 'amazing'];

// Use a for loop to iterate through the languages array
for (let i = 0; i < languages.length; i++) {
  console.log(`${languages[i]} is ${descriptions[i]}`);
}
