// script that updates the text color of the <header> element to red (#FF0000)

//selecting the element
const header = document.querySelector('header');

// Changing text colot to red
header.style.color = '#ff0000'
